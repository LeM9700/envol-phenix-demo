/// Constantes globales de l'application "L'Envol du Phénix"
/// 
/// Cette classe centralise toutes les valeurs constantes utilisées
/// dans l'application pour faciliter la configuration et les modifications.
class AppConstants {
  /// Taille par défaut de la grille de jeu
  /// 
  /// Définit une grille 3x3 comme demandé dans les spécifications.
  /// Cette valeur peut être facilement modifiée pour créer des grilles
  /// plus grandes (ex: 5x5, 8x8) selon les besoins futurs.
  static const defaultGridSize = 3;
}
